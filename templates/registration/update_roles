{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(UserRoles) }}
        {{ form_row(UserRoles.roles) }}
        
        <button type="submit" class="btn">Register</button>
    {{ form_end(UserRoles) }}
{% endblock %}